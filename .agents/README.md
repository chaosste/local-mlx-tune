# Agent skills

## unsloth-buddy

Fine-tuning orchestrator (SFT/DPO/GRPO, eval, export). Installed at:

```
.agents/skills/unsloth-buddy/
```

On Apple Silicon, route training to **mlx-tune** using this repo's scripts and `projects/` output directory. Read `.gaslamp/repo_profile.md` before generating `train.py`.
